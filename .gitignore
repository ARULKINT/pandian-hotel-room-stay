node_modules/
dist/
.env
.env.local
*.log
.DS_Store
backend/data/bookings.json
backend/.env

# Contains a Stitch API key — never commit this.
.agents/

# Pre-rebuild scratch artifacts, not part of the shipped app.
backup_before_stitch_rebuild/
stich file/
